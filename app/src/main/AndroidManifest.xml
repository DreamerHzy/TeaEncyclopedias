<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ckw.teaencyclopedias">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.GuideActivity">
        </activity>
        <activity android:name=".activities.AnimationActivity">
        </activity>
        <activity android:name=".activities.DetailsItemActivity">
        </activity>
        <activity android:name=".activities.HistoryActivity">
        </activity>
        <activity android:name=".activities.CollectionActivity">
        </activity>
        <activity android:name=".activities.CopyRightActivity">
        </activity>
        <activity android:name=".activities.SearchActivity">
        </activity>
    </application>

</manifest>